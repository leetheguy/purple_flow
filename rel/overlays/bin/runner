#!/bin/sh
# Starts this release as the Code node runner instead of the app. See
# specs/100_runner_container.md.
set -eu

cd -P -- "$(dirname -- "$0")"
PURPLEFLOW_ROLE=runner exec ./purple_flow start
